	/*Last Order*/

	var last_order = $(".last_order .owl-calousel");
 
	last_order.owlCarousel({
		singleItem : true,
		slideSpeed : 1000,
		navigation: false,
		pagination: true,
		responsiveRefreshRate : 200,
	});
